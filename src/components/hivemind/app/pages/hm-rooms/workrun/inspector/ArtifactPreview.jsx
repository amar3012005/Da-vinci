import React from 'react';
import ArtifactPreviewRouter from '../artifacts/ArtifactPreviewRouter';

export default function ArtifactPreview({ artifact }) {
  return <ArtifactPreviewRouter artifact={artifact} />;
}
