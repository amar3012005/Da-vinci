import React from 'react';
import MobileShell from '../MobileShell';

/** /hivemind/m/usage — filled in feature-loop phase 3 (F6). */
export default function MobileUsage() {
  return (
    <MobileShell title="Usage">
      <div className="px-5 pt-2 text-[13px] text-[#737373]">Loading usage…</div>
    </MobileShell>
  );
}
